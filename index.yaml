# Spellbook Index
# Generated: 2026-04-22T15:08:07Z
# Do not edit manually. Run: ./scripts/generate-index.sh

skills:
  - name: a11y
    description: "Accessibility audit, remediation, and verification. WCAG 2.2 AA compliance. Three-agent protocol: audit (find issues) → remediate (fix them) → critique (verify fixes). Use when: \"accessibility aud"
    source: first-party
    tags: [2,aa,accessibility,aud,audit,compliance,critique,find,fix,fixes]
  - name: agent-readiness
    description: "Assess and improve codebase readiness for AI coding agents. Dispatches parallel subagents to evaluate style, testing, docs, architecture, CI, observability, security, and dev environment. Produces a s"
    source: first-party
    tags: [a,agents,ai,architecture,assess,ci,codebase,coding,dev,dispatches]
  - name: browser
    description: "Pick the right browser automation tool for a given task — across web and Electron apps, from CI tests to autonomous agents. Browser automation in 2026 is a testing pyramid: deterministic Playwright "
    source: first-party
    tags: [2026,a,across,agents,apps,automation,autonomous,browser,ci,deterministic]
  - name: ceo-review
    description: "Dialectical premise-and-alternatives audit for a plan, spec, or context packet. Four moves: premise challenge (is this the right problem?), mandatory structurally-distinct alternatives, cross-model ou"
    source: first-party
    tags: [a,alternatives,audit,challenge,context,cross-model,dialectical,four,is,mandatory]
  - name: ci
    description: "Audit a repo's CI gates, strengthen what is weak, then drive the pipeline green. Owns confidence in correctness — lint, types, tests, coverage, secrets. Dagger is the canonical pipeline owner; absen"
    source: first-party
    tags: [a,absen,audit,canonical,ci,confidence,correctness,coverage,dagger,drive]
  - name: code-review
    description: "Parallel multi-agent code review. Launch reviewer team, synthesize findings, auto-fix blocking issues, loop until clean. Use when: \"review this\", \"code review\", \"is this ready to ship\", \"check this co"
    source: first-party
    tags: [auto-fix,blocking,check,clean,co,code,findings,is,issues,launch]
  - name: deliver
    description: "Inner-loop composer. Takes one backlog item to merge-ready code. Composes /shape → /implement → {/code-review + /ci + /refactor + /qa} (clean loop) and stops. Does not push, does not merge, does n"
    source: first-party
    tags: [backlog,ci,clean,code,code-review,composer,composes,implement,inner-loop,item]
  - name: demo
    description: "Generate demo artifacts: screenshots, GIF walkthroughs, video recordings, polished launch videos with narration and music. From raw evidence to shipped media. Also handles PR evidence upload via draft"
    source: first-party
    tags: [also,artifacts,demo,draft,evidence,generate,gif,handles,launch,media]
  - name: deploy
    description: "Ship merged code to a deploy target. Thin router — detects target from repo config, dispatches to platform-specific recipe, captures a structured receipt (sha, version, URL, rollback handle), stops "
    source: first-party
    tags: [a,captures,code,config,deploy,detects,dispatches,handle,merged,platform-specific]
  - name: deps
    description: "Analyze, test, and upgrade dependencies. One curated PR, not 47 version bumps. Reachability analysis, behavioral diffs, risk assessment. Package-manager agnostic. Use when: \"upgrade deps\", \"dependency"
    source: first-party
    tags: [47,agnostic,analysis,analyze,assessment,behavioral,bumps,curated,dependencies,dependency]
  - name: diagnose
    description: "Investigate, audit, triage, and fix. Systematic debugging, incident lifecycle, domain auditing, and issue logging. Four-phase protocol: root cause → pattern analysis → hypothesis test → fix. Use"
    source: first-party
    tags: [analysis,audit,auditing,cause,debugging,domain,fix,four-phase,hypothesis,incident]
  - name: flywheel
    description: "Outer-loop shipping orchestrator. Composes /shape, /implement, /yeet, /settle, /ship, and /monitor per backlog item. Closure (archive, reflect, harness routing) lives in /ship; flywheel does not invok"
    source: first-party
    tags: [archive,backlog,closure,composes,flywheel,harness,implement,in,invok,item]
  - name: groom
    description: "Always-on backlog grooming. Tidy, brainstorm, interrogate, investigate, research, and simplify in a single loop. Tidy is not a mode — it happens every time. Strategic-layer work fans out parallel su"
    source: first-party
    tags: [a,always-on,backlog,brainstorm,every,fans,grooming,happens,in,interrogate]
  - name: harness
    description: "Build, maintain, evaluate, and optimize the agent harness — skills, agents, hooks, CLAUDE.md, AGENTS.md, and enforcement infrastructure. Use when: \"create a skill\", \"update skill\", \"improve the harn"
    source: first-party
    tags: [a,agent,agents,build,claude,create,enforcement,evaluate,harn,harness]
  - name: implement
    description: "Atomic TDD build skill. Takes a context packet (shaped ticket) and produces code + tests on a feature branch. Red → Green → Refactor. Does not shape, review, QA, or ship — single concern: spec t"
    source: first-party
    tags: [a,atomic,branch,build,code,concern,context,feature,green,on]
  - name: karpathy-guidelines
    description: "Four behavioral guidelines to reduce common LLM coding mistakes: surface assumptions, prefer simplicity, make surgical changes, drive by verifiable goals. Distilled from Andrej Karpathy's observations"
    source: first-party
    tags: [andrej,assumptions,behavioral,by,changes,coding,common,distilled,drive,four]
  - name: model-research
    description: "Research, compare, and select LLM models for AI-powered apps and workflows. Finds latest models per family, verifies availability on target platform, compares pricing/benchmarks/tool-calling, produces"
    source: first-party
    tags: [ai-powered,apps,availability,benchmarks,compare,compares,family,finds,latest,llm]
  - name: monitor
    description: "Post-deploy signal watch. Poll healthcheck and configured signals through a grace window. Emit structured events. Escalate to /diagnose on trip, close clean on green. Thin watcher, not diagnostician. "
    source: first-party
    tags: [a,clean,close,configured,diagnose,diagnostician,emit,escalate,events,grace]
  - name: office-hours
    description: "YC-partner-style interrogation of a raw idea. Six forcing questions before you shape anything: demand reality, status quo, desperate specificity, narrowest wedge, observation & surprise, future-fit. N"
    source: first-party
    tags: [a,anything,before,demand,desperate,forcing,future-fit,idea,interrogation,n]
  - name: qa
    description: "Browser-based QA, exploratory testing, evidence capture, and bug reporting. Drive running applications and verify they work — not just that tests pass. Use when: \"run QA\", \"test this\", \"verify the f"
    source: first-party
    tags: [applications,browser-based,bug,capture,drive,evidence,exploratory,f,just,pass]
  - name: refactor
    description: "Branch-aware simplification and refactoring workflow. On feature branches, compare against base and simplify the diff before merge. On primary branch, scan the full codebase, research prior art, and i"
    source: first-party
    tags: [against,art,base,before,branch,branch-aware,branches,codebase,compare,diff]
  - name: reflect
    description: "Session retrospective, operator coaching, harness postmortem, codification, and outer-loop cycle critique. Distills learnings into hooks/rules/skills, mutates the backlog from evidence, and emits harn"
    source: first-party
    tags: [backlog,coaching,codification,critique,cycle,distills,emits,evidence,harn,harness]
  - name: research
    description: "Web research, multi-AI delegation, and multi-perspective validation. /research [query], /research delegate [task], /research thinktank [topic]. Triggers: \"search for\", \"look up\", \"research\", \"delegate"
    source: first-party
    tags: [delegate,delegation,look,multi-ai,multi-perspective,query,research,search,task,thinktank]
  - name: seed
    description: "Install a default harness into this repo. Copy most spellbook primitives (skills, agents, philosophy bench) into a repo-local shared skill layer with no filtering or tailoring, then bridge harness-spe"
    source: first-party
    tags: [a,agents,bench,bridge,copy,default,filtering,harness,harness-spe,install]
  - name: settle
    description: "Polish loop. Take a feature branch with code and tests and iterate on it via /ci, /code-review, and /refactor until the branch is lean, green, and ship-ready. Stops at merge-ready. Does not merge. Doe"
    source: first-party
    tags: [a,at,branch,ci,code,code-review,doe,feature,green,is]
  - name: shape
    description: "Shape a raw idea into something buildable. Product + technical exploration. Spec, design, critique, plan. Output is a context packet. Use when: \"shape this\", \"write a spec\", \"design this feature\", \"pl"
    source: first-party
    tags: [a,buildable,context,critique,design,exploration,feature,idea,is,output]
  - name: ship
    description: "Final mile. Take a merge-ready branch to shipped: squash-merge, archive the backlog ticket(s) with Closes-backlog trailers preserved into the merge commit, update touched docs, run /reflect, and apply"
    source: first-party
    tags: [a,apply,archive,backlog,branch,closes-backlog,commit,docs,final,merge]
  - name: tailor
    description: "Tailor this repository's harness. Explore the repo, read prior session history, browse the spellbook catalog, and install a per-repo set of skills into a shared repo-local skill layer, with harness-sp"
    source: first-party
    tags: [a,browse,catalog,explore,harness,harness-sp,history,install,layer,of]
  - name: yeet
    description: "End-to-end \"ship it to the remote\" in one command. Reads the whole worktree, understands what's in flight, tidies debris, splits pending work into semantically-meaningful conventional commits, and pus"
    source: first-party
    tags: [command,commits,conventional,debris,end-to-end,flight,in,it,one,pending]

agents:
  - name: a11y-auditor
    description: "Finds accessibility issues. Does NOT fix them. Read-only investigation."
  - name: a11y-critic
    description: "Verifies accessibility fixes. Skeptical by default. Tests keyboard, axe, screen reader semantics."
  - name: a11y-fixer
    description: "Fixes accessibility issues from audit findings. Minimal surgical changes. Native HTML over ARIA."
  - name: beck
    description: "TDD + simple design - \"Red. Green. Refactor\""
  - name: builder
    description: "Implements specs via TDD. Follows the planner's context packet exactly. Heads-down execution."
  - name: carmack
    description: "Direct implementation + shippability - \"Focus is deciding what NOT to do\""
  - name: critic
    description: "Evaluates builder output against grading criteria. Skeptical by default. Fails or approves."
  - name: grug
    description: "Complexity demon hunter - \"complexity very, very bad\""
  - name: ousterhout
    description: "Deep modules + information hiding - \"Deep modules manage complexity\""
  - name: planner
    description: "Decomposes work into buildable specs. Writes context packets. Does NOT implement."

# Collections are defined in collections.yaml
